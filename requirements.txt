# RAG Chatbot — Production RAG + Agentic RAG + Multilingual RAG + RLHF + Voice
# Install: pip install -r requirements.txt

# Groq API (Llama inference — free tier)
groq>=0.9.0

# LangChain
langchain>=0.2.0
langchain-community>=0.2.0
langchain-groq>=0.1.0
langchain-huggingface>=0.0.3

# Vector store
faiss-cpu>=1.8.0

# Embeddings (free, local — multilingual-MiniLM-L12-v2 supports 50+ languages)
sentence-transformers>=3.0.0

# BM25 sparse retrieval (Layer 3 hybrid)
rank-bm25>=0.2.2

# Language detection (for auto-detecting query language in multilingual RAG)
langdetect>=1.0.9

# Document loaders
pypdf>=4.0.0
docx2txt>=0.8
pdfplumber>=0.11.0

# Streamlit UI
streamlit>=1.35.0

# Voice-to-text mic recorder (optional — gracefully disabled if unavailable)
streamlit-mic-recorder>=0.0.4

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.7.0
